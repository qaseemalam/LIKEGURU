<%= form_for(@fbclickdatum) do |f| %>
  <% if @fbclickdatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fbclickdatum.errors.count, "error") %> prohibited this fbclickdatum from being saved:</h2>

      <ul>
      <% @fbclickdatum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Like_Id %><br />
    <%= f.text_field :Like_Id %>
  </div>
  <div class="field">
    <%= f.label :Like_category %><br />
    <%= f.text_field :Like_category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
